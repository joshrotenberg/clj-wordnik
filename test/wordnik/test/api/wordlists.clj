(ns wordnik.test.api.wordlists
  (:use [wordnik.core]
        [wordnik.util]
        [wordnik.api wordlists account]
        [wordnik.test.properties]
        [clojure.data.json :as json]
        [clojure.test]))

;; see wordnik.test.api.wordlist.clj for the call to wordlists
